{
  "buildCommand": "cd ngo-frontend && npm install && npm run build",
  "outputDirectory": "ngo-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
